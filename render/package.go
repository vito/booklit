// Package render contains conversions from a processed Booklit section into a
// rendered format such as plaintext or HTML, either for serving in a browser
// or for writing to files on disk.
package render
