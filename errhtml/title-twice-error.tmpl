<div class="error">
  <div class="error-message">cannot set title twice</div>

  <div class="code-location">
    {{.ErrorLocation | annotate}}
  </div>

  <p>The section's title was first set here:</p>

  <div class="code-location">
    {{.TitleLocation | annotate}}
  </div>

  <p>Maybe the second <code>\title</code> should be in a <code>\section{...}</code>?</p>
</div>
